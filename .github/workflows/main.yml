name: Docker Auto Deploy VM

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Build Docker Image
      run: docker build -t hello-world .

    - name: Tagging Docker Image
      run: docker tag hello-world kavita346/hello-world
      
    - name: Login to GitHub Container Registry
      run: echo "${{ secrets.docker_passord_token }}" | docker login -u "${{ secrets.docker_username_sai40 }}" --password-stdin
